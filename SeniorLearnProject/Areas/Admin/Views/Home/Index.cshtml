@using SeniorLearnProject.Models.Identity;
@{
    ViewData["Title"] = "Home Page";
}
@model List<User>

<div class="text-center">
    <h2>The following users are awaiting approval</h2>
    <ul>
        @foreach(var u in Model){
            <form>
                <p>@u.UserName</p>
                
            </form>
        }
    </ul>
</div>
